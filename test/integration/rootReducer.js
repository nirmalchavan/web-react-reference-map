import { combineReducers } from 'redux';
import mapReducer from './../../src/reducers/mapReducer';

const creditCardRootReducer = combineReducers({
    mapReducer
});

export default creditCardRootReducer;
